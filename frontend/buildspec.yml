version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo "Installing frontend dependencies..."
      - npm install
  build:
    commands:
      - echo "Building React frontend..."
      - npm run build
artifacts:
  files:
    - '**/*'
    - '!node_modules/**'
